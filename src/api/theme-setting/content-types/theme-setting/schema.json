{
  "kind": "singleType",
  "collectionName": "theme_settings",
  "info": {
    "singularName": "theme-setting",
    "pluralName": "theme-settings",
    "displayName": "Theme Setting",
    "description": "Site-wide brand colors. Editing and publishing here recolors the live site within ~60 seconds — no code change needed."
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "brandColor": {
      "type": "string",
      "customField": "global::color",
      "required": true,
      "default": "#0324FF",
      "description": "Primary brand color — used for buttons, links, nav highlights"
    },
    "accentColor": {
      "type": "string",
      "customField": "global::color",
      "required": true,
      "default": "#FFA31A",
      "description": "Accent color — used for secondary CTAs and highlights"
    },
    "inkColor": {
      "type": "string",
      "customField": "global::color",
      "required": true,
      "default": "#000000",
      "description": "Near-black UI color — used for dark buttons, footer, dark text"
    }
  }
}
