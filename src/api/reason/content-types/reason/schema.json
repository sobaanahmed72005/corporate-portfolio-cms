{
  "kind": "collectionType",
  "collectionName": "reasons",
  "info": {
    "singularName": "reason",
    "pluralName": "reasons",
    "displayName": "Why Choose Us Reason",
    "description": "The reason tiles shown in the \"Why Choose Us\" section on the home page"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "title": {
      "type": "string",
      "required": true,
      "maxLength": 100,
      "description": "The short headline for this reason, e.g. \"Certified Technicians\""
    },
    "description": {
      "type": "text",
      "required": true,
      "maxLength": 400,
      "description": "One or two sentences explaining this reason in more detail"
    },
    "tag": {
      "type": "string",
      "required": true,
      "maxLength": 60,
      "description": "Short badge text under the description, e.g. \"TRUSTED SOURCING\""
    },
    "icon": {
      "type": "string",
      "required": true,
      "maxLength": 50,
      "description": "Key into the frontend's Icon.tsx registry"
    },
    "iconColor": {
      "type": "string",
      "customField": "global::color",
      "required": true,
      "default": "#3B82F6",
      "description": "Icon badge color for this reason tile"
    },
    "image": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": ["images"],
      "description": "Optional banner photo shown on this reason tile — falls back to the icon badge when unset"
    }
  }
}
